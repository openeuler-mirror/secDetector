syntax = "proto3";

package pubsub;

// The message type
message Message {
  string text = 1;
}

// The service definition
service PubSub {
  rpc Subscribe (SubscribeRequest) returns (stream Message) {}
}

message SubscribeRequest {
  string topic = 1;
}
